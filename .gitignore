# nunca subir para o repositório público:
build.js
fonts-inline.css
*.local.html

# conteúdo sensível (folha salarial, nomes) — NUNCA commitar:
seed.html
*-backup.json
inspira-conteudo-backup*.json

# pipeline de migração aposentado (não roda: o HTML-fonte não existe mais)
site-src/
build-firebase.js

# firestore.rules NÃO entra aqui: é infraestrutura como código, sem credencial,
# e é a única camada que protege o balanço financeiro. Precisa de diff e histórico.

# Firecrawl scrape cache
.firecrawl/
